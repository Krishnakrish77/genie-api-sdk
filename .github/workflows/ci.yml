name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  changes:
    name: Detect SDK changes
    runs-on: ubuntu-latest
    outputs:
      sdk: ${{ steps.filter.outputs.sdk }}
      web_example: ${{ steps.filter.outputs.web_example }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            sdk:
              - "python/**"
              - "typescript/**"
            web_example:
              - "examples/web-chat/**"

  python:
    name: Python 3.14
    needs: changes
    if: needs.changes.outputs.sdk == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: python/pyproject.toml
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]" build twine
      - run: python -m pytest -q tests
      - run: python -m compileall -q src
      - run: python -m build
      - run: python -m twine check dist/*

  typescript:
    name: TypeScript / Node 26
    needs: changes
    if: needs.changes.outputs.sdk == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: typescript
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: typescript/package-lock.json
      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm pack --dry-run

  web-example:
    name: Web example
    needs: changes
    if: needs.changes.outputs.web_example == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: examples/web-chat
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: typescript/package-lock.json
      - run: npm ci
        working-directory: typescript
      - run: npm run build
        working-directory: typescript
      - run: npm install --no-save --no-package-lock ../../typescript
      - run: npm test
